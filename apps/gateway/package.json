{
  "name": "@led-control/gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @led-control/shared build && tsx src/index.ts",
    "bluez:probe": "tsx scripts/bluez-capability-probe.ts",
    "test": "pnpm --filter @led-control/shared build && vitest run",
    "test:contracts": "node --test docker/*.test.mjs",
    "lint": "tsc --noEmit",
    "typecheck": "pnpm --filter @led-control/shared build && tsc --noEmit"
  },
  "dependencies": {
    "@homebridge/dbus-native": "^0.7.7",
    "@led-control/shared": "workspace:*",
    "dotenv": "^16.4.7",
    "mqtt": "^5.10.3"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
